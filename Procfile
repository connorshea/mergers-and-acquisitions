# Toolforge Build Service web process. `toolforge webservice buildservice start`
# runs this. The app listens on $PORT (set by Toolforge) and serves both the API
# and the built SPA from dist/client.
web: npm run start
