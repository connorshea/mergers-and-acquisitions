# drizzle-kit schema snapshots: regenerated by `pnpm db:generate`, never edited
# by hand. Collapse them in GitHub diffs (the .sql migration and _journal.json
# stay visible, those are what to review).
db/migrations/meta/*_snapshot.json linguist-generated=true
