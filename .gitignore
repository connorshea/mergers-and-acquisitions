node_modules
dist
*.tsbuildinfo

# Local environment config (keep .env.example tracked)
.env
.env.local
.void
.wrangler
.DS_Store

# Local Wikidata dump(s) used to seed dev data — too large to commit (~250MB).
seed-data/
